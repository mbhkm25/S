# Remaining Production Cleanup Items

- Classify and decide retention for analyzer/model benchmark endpoints.
- Classify and decide retention for assistant canary/shadow/eval endpoints.
- Classify preview diagnostics and smoke/corpus runners.
- Remove or archive the completed leaked-password one-shot workflow when connector write policy permits.
- Re-run production security/quality checks after final cleanup merge.
- Close the cleanup gate in SANAD OS only after the above are resolved.
