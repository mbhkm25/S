# One-shot production deployment

This temporary change triggers the guarded production deployment for the profile-completion contract release. The workflow is intentionally path-scoped to its own file and will be removed after the deployment result is verified.
