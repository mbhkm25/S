name: Apply analyzer temporal contract

on:
  push:
    branches:
      - agent/operation-time-file-access-relationship-hardening
    paths:
      - scripts/apply-analyzer-temporal-contract.mjs
      - .github/workflows/apply-analyzer-temporal-contract.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/operation-time-file-access-relationship-hardening
          fetch-depth: 0
      - run: node scripts/apply-analyzer-temporal-contract.mjs
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Write explicit temporal fields from operation analyzer"
          git push origin HEAD:agent/operation-time-file-access-relationship-hardening
